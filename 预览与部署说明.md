# 预览与部署说明

## 本地预览（推荐）

用浏览器直接打开 `index.html` 或 `beike_analysis_v2.html` 时，部分功能可能受限于 `file://` 协议。建议用本地 HTTP 服务预览，链接即可正常点击跳转。

### 方式一：用 Node 运行（无需安装，一条命令）

在项目目录下执行：

```bash
npm run preview
```

然后打开浏览器访问：**http://localhost:3000**

- 根路径会跳转到主报告页
- 主报告里「分事业线」卡片可点击进入各事业线详情
- 详情页「← 返回总览」可回到主报告

### 方式二：用 Python 运行

若已安装 Python，在项目目录下执行：

```bash
# Python 3
python -m http.server 8080
```

然后打开：**http://localhost:8080**

---

## 把项目推送到 GitHub 仓库

### 第一步：在 GitHub 上创建仓库

1. 打开 [github.com](https://github.com) 并登录，在首页左侧或右上角找到 **Create repository**（创建仓库）或 **Create project**（创建项目）。
2. 填写仓库名（例如 `aiexplosure`），选择 **Public**，**不要**勾选「Add a README」。
3. 点击 **Create repository**，创建完成后会看到空仓库的说明页，记下仓库地址（形如 `https://github.com/你的用户名/aiexplosure.git`）。

### 第二步：在本地初始化并推送

在项目目录 `aiexplosure` 下打开终端，依次执行（把下面的仓库地址换成你自己的）：

```bash
# 初始化 Git
git init

# 添加所有文件
git add .

# 第一次提交
git commit -m "初始提交：贝壳 AI Exposure 分析报告"

# 添加远程仓库
git remote add origin https://github.com/zhuzhujuly718-oss/aiexplosure.git

# 推送到 main 分支
git branch -M main
git push -u origin main
```

若使用 **Import repository**（导入仓库）：在 GitHub 选择「Import」后，填写你本地仓库的地址（需先推到其他可访问的 Git 地址），或按上面步骤先在本地建好再 `git remote add` + `git push` 即可。

---

## 部署到网上（可分享链接）

项目是纯静态 HTML，可部署到任意静态托管。

### GitHub Pages

1. 把本项目推送到 GitHub 仓库
2. 仓库设置 → Pages → Source 选 **main**（或你的默认分支），目录选 **/ (root)**
3. 保存后等待生成，访问 `https://<你的用户名>.github.io/<仓库名>/` 即可

### Vercel

1. 打开 [vercel.com](https://vercel.com)，用 GitHub 登录
2. 选择「Import」导入本仓库
3. 无需改配置，直接 Deploy；完成后会得到一个可访问的网址

### 其他

也可上传到 **Netlify**、**Cloudflare Pages** 或公司内网静态服务器，只要把当前目录下所有文件上传到网站根目录即可。
